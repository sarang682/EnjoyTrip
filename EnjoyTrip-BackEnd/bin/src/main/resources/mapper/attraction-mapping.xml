<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.model.dao.AttractionDao">
	
	<select id="listSido" resultType="sido">
		<![CDATA[
			select * from sido
		]]>
	</select>
	
	<select id="listGugun" parameterType="int" resultType="gugun">
		<![CDATA[
			select *
			from gugun
			where sido_code = #{sido_code}
		]]>
	</select>
	
	<select id="listAttractionInfo" resultType="attractionInfo" parameterType="int">
		<![CDATA[
			SELECT * FROM attraction_info where sido_code=#{sido_code} and gugun_code=#{gugun_code}
		]]>
	</select>
	
	<select id="attractionInfo" resultType="attractionInfo" parameterType="int">
		<![CDATA[
			SELECT * FROM attraction_info where content_id=#{content_id}
		]]>
	</select>
	
	<select id="attractionDescription" resultType="attractionDescription" parameterType="int">
		<![CDATA[
			SELECT * FROM attraction_description where content_id=#{content_id}
		]]>
	</select>
</mapper>