<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.model.dao.BoardDao">
	<insert id="insert" parameterType="board">
		<![CDATA[
			insert into board(user_id, subject, content)
			values(#{user_id}, #{subject}, #{content})
		]]>
	</insert>
	
	<select id="list" resultType="board">
		<![CDATA[
			select * from board
		]]>
	</select>
	
	<update id="updateHit" parameterType="int">
		<![CDATA[
			update board
			set hit = (select temp.hit
						from (select hit
								from board
								where article_no = #{article_no}) temp) + 1
			where article_no = #{article_no}
		]]>
	</update>
	
	<select id="board" resultType="board" parameterType="Integer">
		 <![CDATA[
		 	select *
		 	from board
		 	where article_no = #{article_no}
		]]>
	</select>
	
	<update id="update" parameterType="board">
		<![CDATA[
			update board
			set subject = #{subject}, content = #{content}
			where article_no = #{article_no}
		]]>
	</update>
	
	<delete id="delete" parameterType="Integer">
		<![CDATA[
			delete
			from board
			where article_no = #{article_no}
		]]>
	</delete>
</mapper>