<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrip.model.dao.BoardDao">

	<resultMap type="board" id="boardResult">
        <result column="article_no" property="articleNo"/>
        <result column="user_id" property="userId"/>
        <result column="subject" property="subject"/>
        <result column="content" property="content"/>
        <result column="hit" property="hit"/>
        <result column="register_time" property="registerTime"/>
    </resultMap>
    
	<insert id="insert" parameterType="board">
		<![CDATA[
			insert into board(user_id, subject, content)
			values(#{userId}, #{subject}, #{content})
		]]>
	</insert>
	
	<select id="list" resultMap="boardResult">
		<![CDATA[
			select * from board
		]]>
	</select>
	
	<update id="updateHit" parameterType="int">
		<![CDATA[
			update board
			set hit = (select temp.hit
						from (select hit
								from board
								where article_no = #{articleNo}) temp) + 1
			where article_no = #{articleNo}
		]]>
	</update>
	
	<select id="selectByArticleNo" resultMap="boardResult" parameterType="int">
		 <![CDATA[
		 	select *
		 	from board
		 	where article_no = #{articleNo}
		]]>
	</select>
	
	<update id="update" parameterType="board">
		<![CDATA[
			update board
			set subject = #{subject}, content = #{content}
			where article_no = #{articleNo}
		]]>
	</update>
	
	<delete id="delete" parameterType="int">
		<![CDATA[
			delete
			from board
			where article_no = #{articleNo}
		]]>
	</delete>
</mapper>